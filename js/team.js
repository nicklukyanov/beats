let list = document.getElementById("teamList");

const nodeCollect = document.querySelectorAll(".team__name");
const htmlCollect = document.getElementsByClassName("team__name");
const arr = [1, 2, 3, 4];

arr.forEach(function name(element) {
  console.log(element);
})


nodeCollect.forEach(function name(element) {
  console.log(element);
})

htmlCollect.forEach(function name(element) {
  console.log(element);
})


